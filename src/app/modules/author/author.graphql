type Author {
  id: ID! # the ! means that every author object _must_ have an id
  firstName: String
  lastName: String
  name: String
  """
  the list of Posts by this author
  """
  posts: [Post]
  votes: Int
}
